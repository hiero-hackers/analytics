query ISSUES_QUERY($owner:String!,$repo:String!,$cursor:String,$states:[IssueState!]){
  repository(owner:$owner,name:$repo){
    issues(first:100, after:$cursor, states:$states){
      pageInfo{
        hasNextPage
        endCursor
      }
      nodes{
        ...IssueFields
      }
    }
  }
  rateLimit{
    limit
    remaining
    cost
    resetAt
  }
}
