"""GitHub data-source layer: client, ingestion, models, caching, and stores.

Import from the owning submodule directly (``data_sources.github_client``,
``data_sources.github_ingest``, ``data_sources.models``, ...) — this package
deliberately re-exports nothing, so each name has exactly one import path.
"""
